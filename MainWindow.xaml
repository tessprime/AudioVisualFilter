<Window x:Class="AudioVisualFilter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="OutputTextBox"
             Grid.Row="0"
             Background="Transparent"
             Foreground="White"
             FontSize="24"
             IsReadOnly="False"
             BorderThickness="0"
             HorizontalAlignment="Right"
             TextAlignment="Right"
             Text="Hello"/>
        <Canvas x:Name="MainCanvas"     Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Width="400" Height="200" ClipToBounds="True"/>
        <Canvas x:Name="FormantCanvas"  Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Top" Width="400" Height="150" ClipToBounds="True"/>
    </Grid> <!-- 0x80 alpha = ~50% red tint -->

</Window>